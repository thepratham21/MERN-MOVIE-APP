version: "3.8"

services:
  frontend:
    image: thepratham21/mern-movie-app-frontend:latest
    container_name: frontend

  backend:
    image: thepratham21/mern-movie-app-backend:latest
    container_name: backend
    env_file:
      - ./backend/.env

  nginx:
    image: nginx:alpine
    container_name: nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - frontend
      - backend
